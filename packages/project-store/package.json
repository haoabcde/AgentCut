{
  "name": "@agentcut/project-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && vitest run src"
  },
  "dependencies": {
    "@agentcut/edit-commands": "workspace:*",
    "@agentcut/timeline-schema": "workspace:*"
  }
}
