{
  "name": "@agentcut/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agentcut-mcp": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @agentcut/agent-client build && pnpm run build && vitest run src"
  },
  "dependencies": {
    "@agentcut/agent-client": "workspace:*",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@agentcut/project-store": "workspace:*",
    "@agentcut/reference-host": "workspace:*",
    "@modelcontextprotocol/client": "2.0.0"
  }
}
