# AgentCut repository instructions

本文件适用于整个 AgentCut 仓库，并补充会话级工作协议。

## 开发记录

- 每次完成有效的代码、测试、架构、依赖、配置或产品文档修改时，必须在同一批改动中更新 `docs/11-development-log.md`。
- 记录必须包含日期、目标、实际改动、验证结果以及仍存在的限制；不能只写笼统的“优化”或“修复”。
- 纯格式整理或开发记录自身的文字修正可以并入最近一条记录，不为日志更新递归新增日志。
- 对尚未验证的能力明确标为待验证，不得把计划写成完成结果。

## 完成检查

- 提交前检查本次变更范围，避免纳入缓存、构建产物、凭据、媒体原片或无关文件。
- 代码改动默认运行 `pnpm check`；若无法运行，必须在开发记录和最终汇报中写明原因与替代检查。
- 未经用户明确要求，不发布、部署、创建正式 release 或修改仓库可见性。
