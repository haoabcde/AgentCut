# AgentCut 通用剪辑工作区 Design QA

## QA 范围

- 目标：保证通用剪辑工作区在 664 × 749 的窄窗口内仍可访问编辑助手、文稿、画面预览和时间线，并维持现有桌面视觉语言。
- 状态：真实 `sample-03` dogfood 工程，REV 9，默认停留在文稿视图。
- 参考截图：`.agentcut/qa/general-workspace-before.png`。
- 实现截图：`.agentcut/qa/general-workspace-after.png`。
- 并排比较：`.agentcut/qa/general-workspace-comparison.png`。

## 比较结论

### Pass 1

- P1：旧布局强制最小宽度，窄窗口只显示编辑助手与部分文稿；Viewer 和时间线被裁出视口，用户无法抵达。
- 修正：在不改变宽屏四区布局的前提下，为 1180 px 以下窗口加入助手、文稿、画面、时间线四个工作区 Tab；一次只展示一个主面板。
- 修正：移除窄窗口的强制横向画布，文稿在完整可用宽度内重排。

### Pass 2

- 四个工作区 Tab 均可通过可访问名称定位，选中状态与实际可见面板一致。
- 画面预览、时间线和文稿完成真实点击切换；时间线点击中点后视频定位到 38.7 秒，时钟更新为 `0:38 / 1:17`。
- 时间线具有与视频 `currentTime` 同步的播放头；候选按钮仍位于可点击定位层之上。
- 实现截图与参考截图在同一视口、同一工程、同一默认文稿状态下并排检查；字体、颜色、边框、候选标记和文稿密度保持一致，未发现裁切、错误间距或不可达主功能。
- 页面运行日志为空。

## 剩余边界

- 本轮验证的是窄桌面窗口，不代表移动端产品适配；AgentCut Alpha 仍以 macOS 桌面为目标。
- 当前紧凑模式一次展示一个主面板，尚未提供用户可调分栏或面板停靠。

## 2026-07-31：Alpha 验收面板

- 在正式 sample-03 revision 9 上展开编辑助手中的“Alpha 验收”，页面显示候选 0/7、边界 0/3 和精确 REV/hash；正式工程没有写入合成的人类判断。
- 点击首个“试听剪后边界”后，Viewer 自动切换到“当前初剪”，从边界前开始播放；证明面板可以调用现有 PreviewPlan，而不是另算一套 source time。
- 在 sample-03 临时副本上写入候选“误报”和边界“截断音节”，进度变为 1/7 与 1/3；整页刷新后判断仍恢复，工程 revision 保持 9。
- 同视口截图人工检查通过：折叠面板保持在 Agent 编辑助手内，没有把工作区重新退化为独立口播审阅台；Viewer、文稿和时间线仍是并列主区。浏览器 warning/error 日志为空。
- 本轮只验证标注可达性、布局和持久化；真实候选语义是否正确、3 个边界是否可用仍必须由用户实际听审，不能由界面 QA 代替。

final result: passed
